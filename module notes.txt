General notes:

-- List of parameters start with '?'
-- The query parameter for passing simple message in the URL is: 'mymsg'
-- The query parameter for the room (in the URL) is: 'room'
-- To add another parameter use '&' in the URL

Module 12 Notes:

-- Install socket.io:                       npm install socket.io@4.7.2
-- Change the database environment variable 
    DATABASE_URL=mongodb://localhost:27017/blog  
    to
    DATABASE_URL=mongodb://localhost:27017/chat
-- Integrate the node server in backen/src/app.js
    -- add:
-- Frontend:
    -- Install the client for the frontend:     npm install socket.io-client@4.7.2

 -- Broadcast to everyone but the sender ===================
    -- socket.broadcast.emit('chat.message', { username: socket.id, message })